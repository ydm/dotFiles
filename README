My dotFiles.

These include configurations for:
        emacs
        git (see ./Makefile)
        xmonad

./Emacs.gitignore is retrieved from https://github.com/github/gitignore

Emacs config includes .emacs, a lot of el files and these plugins:
        yasnippet (retrieved from github)
        haskell-mode (retrieved from github)
        org-mode (included)
        auto-complete (manually retrieved for now... TODO!)

To install:
        make install
To uninstall:
        make uninstall

^_^
